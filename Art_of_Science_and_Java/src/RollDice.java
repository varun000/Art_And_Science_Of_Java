/*
 * File: RollDice.java
 * -------------------------------------------
 * This program simulates rolling sum number of dice until
 * the maximum value on all the dice is rolled.
 */

import acm.program.*;
import acm.util.*;

public class RollDice extends ConsoleProgram {
	
	/* Number of sides on each die. */
	private static final int NUM_SIDES = 6;
	
	public void run() {
		int numDice = readInt ("Number of Dice: ");
		int maxRoll = numDice * NUM_SIDES;
		int numRolls = 0;
		
		while (true) {
			
		}
	}
}
